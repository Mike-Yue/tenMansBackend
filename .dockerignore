.git
.gitignore
*.exe
*.exe~
# Local dev database (and its WAL sidecars). The container seeds from the
# embedded db/seed.db onto the persistent disk instead, so this must NOT ship.
production.db
production.db-wal
production.db-shm
*.md
